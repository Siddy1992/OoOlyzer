# Physical validation experiments

This directory contains experiments that were performed on physical hardware and are **not** part of the gem5 simulator workflow.

Currently included:

```text
p550_present80/
```

The P550 experiment must be evaluated using its archived traces/results, its analysis-only reproduction script, or a physical rerun on a compatible SiFive P550 system. See `../docs/P550_PHYSICAL_VALIDATION.md`.
